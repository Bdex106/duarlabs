services:
  - type: web
    name: duarlabs
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npx serve dist -p $PORT -c ../serve.json
    envVars:
      - key: NODE_VERSION
        value: 20
